# Secrets — never commit
.env
.streamlit/secrets.toml

# Plaintext school documents — only encrypted version (.enc) goes to repo
combined_context.md

# Local-only scripts (contain keys or local paths)
refresh_context.py
audio_worker.py
audio_worker.log
audio_state.json
audio_worker.pid
crawl_and_prepare.py
ofsted_agent.py
OfstedAgent.icns
mlx_models/

# Python
__pycache__/
*.pyc
*.pyo
.venv/
