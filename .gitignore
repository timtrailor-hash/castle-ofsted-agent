# Secrets — never commit
.env
.streamlit/secrets.toml
credentials.py

# Plaintext school documents — only encrypted version (.enc) goes to repo
combined_context.md

# Local-only scripts (contain keys or local paths)
refresh_context.py
audio_worker.py
audio_worker.log
audio_state.json
audio_worker.pid
crawl_and_prepare.py
ofsted_agent.py
OfstedAgent.icns
mlx_models/

# Google Drive sync (local-only — tokens contain secrets, links are generated)
gdrive_token.json
gdrive_links.json
sync_to_gdrive.py

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
